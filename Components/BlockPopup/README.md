# Block Popup

Modal popup with image and text side by side, modeled after Block Image Text. Opens automatically on page load (with optional delay), can be closed via the close button, ESC key, or backdrop click. Dismissal is remembered per browser via `localStorage` keyed on the configured Popup ID — change the ID in the editor to re-show the popup to users who already dismissed it.
