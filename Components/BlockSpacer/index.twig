<flynt-component name="BlockSpacer" style="{% if options.percentageDistance starts with '-' %}height: 0; margin-block: {{ options.percentageDistance }};{% else %}height: {{ options.percentageDistance }};{% endif %}"></flynt-component>
