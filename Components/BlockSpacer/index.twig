<flynt-component name="BlockSpacer" class="{% if options.topBorder %}border-t-2 border-black{% endif %}" style="{% if options.percentageDistance starts with '-' %}height: 0; margin-block: {{ options.percentageDistance }};{% else %}height: {{ options.percentageDistance }};{% endif %}"></flynt-component>
