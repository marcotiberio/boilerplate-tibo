# Block Embed

Same layout and styling as Block WYSIWYG, but with a raw code field for third-party embeds (`<iframe>`, widget scripts, …) that would otherwise be stripped by content escaping. Optional text above the embed, plus the usual position, background and button options.
